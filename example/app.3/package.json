{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
      "clean": "rm -rf pub && rm -rf *.log && rm -rf names.json",
      "server": "npm run deploy && puer",
      "deploy":"npm run clean && node ../../bin/build.js ./nejpack.conf",
      "dev":"browser-sync start --directory --server ."
  },
  "author": "dong.wang",
  "license": "MIT"
}
